#include <stdint.h>

// int foo(int baa) { return baa++; }

int main() {
  // //declare
  int i, j, h;

  // //initialize
  i = 2;
  j = i;

  int z = 0;
  int v = 1;
  int b = z * v;

  // // integer operations
  h = i + j;
  // h = i * j;
  // h = i - j;
  // h = i / j;
  // h = i | j;
  // h = i & j;
  // h = i ^ j;
  // h = i >> j;
  // h = i << j;
  // h = i || j;
  // h = i && j;
  // h = i % j;

  // // for loop

  // for (int k = 0; k < 10; k++) {
  //    i += k;
  // }

  // if (i < 6) {
  //     h = 3;
  // }

  // else {
  //     h = 4;
  // }

  // h = foo(h);
  return 0;
}
