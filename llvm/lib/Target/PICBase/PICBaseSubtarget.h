#ifndef __PROJECTS_LLVM_LLVM_PIC_LLVM_LIB_TARGET_PICBASE_PICBASESUBTARGET_H_
#define __PROJECTS_LLVM_LLVM_PIC_LLVM_LIB_TARGET_PICBASE_PICBASESUBTARGET_H_

#include "llvm/CodeGen/GlobalISel/InlineAsmLowering.h"
#include "llvm/CodeGen/GlobalISel/InstructionSelector.h"
#include "llvm/CodeGen/GlobalISel/Utils.h"
#include "llvm/CodeGen/Register.h"
#include "llvm/CodeGen/TargetSubtargetInfo.h"
#include "llvm/IR/DataLayout.h"
#include "llvm/Target/TargetMachine.h"

#define GET_SUBTARGETINFO_HEADER
#include "PICBaseGenSubtargetInfo.inc"

namespace llvm {

class PICBaseTargetMachine;

class PICBaseSubtarget : public PICBaseGenSubtargetInfo {
public:
  explicit PICBaseSubtarget(const Triple &TT, const std::string &CPU,
                            const std::string &FS,
                            const PICBaseTargetMachine &TM);

  /// Parses a subtarget feature string, setting appropriate options.
  /// \note Definition of function is auto generated by `tblgen`.
  void ParseSubtargetFeatures(StringRef CPU, StringRef TuneCPU, StringRef FS);

private:
  bool StaticStack = false;
  bool HasBaseInstrs = false;

  // Dummy member, used by FeatureSet's. We cannot have a SubtargetFeature with
  // no variable, so we instead bind pseudo features to this variable.
  bool FeatureSetDummy = false;
};

} // namespace llvm

#endif // __PROJECTS_LLVM_LLVM_PIC_LLVM_LIB_TARGET_PICBASE_PICBASESUBTARGET_H_